#include <iostream>
#include <fstream>
#include <string>
using namespace std;
//ofstream to write data to file
//if stream to read data fromfile
//fstream
int main () {
  string text;
  fstream file;
  // file.open("text.txt",ios::out);
  // file<<"hgjh jhfgh jhghj";
  file.open("text.txt",ios::in);
  while(getline(file,text)){
    cout<<text<<endl;
  }
  file.close();
  return 0;
}